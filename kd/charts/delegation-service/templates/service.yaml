---
# Source: kd/charts/delegation-service/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: delegation-service
  labels:
    app.kubernetes.io/name: delegation-service
    helm.sh/chart: delegation-service-0.1.0
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/managed-by: Tiller
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: delegation-service
    app.kubernetes.io/instance: release-name