---
# Source: kd/charts/quota-ui/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: quota-ui
  labels:
    app.kubernetes.io/name: quota-ui
    helm.sh/chart: quota-ui-0.1.0
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/managed-by: Tiller
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: quota-ui
    app.kubernetes.io/instance: release-name