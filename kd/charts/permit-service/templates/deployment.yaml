---
# Source: kd/charts/permit-service/templates/deployment.yaml
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: permit-service
  labels:
    app.kubernetes.io/name: permit-service
    helm.sh/chart: permit-service-0.1.0
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/managed-by: Tiller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: permit-service
      app.kubernetes.io/instance: release-name
  template:
    metadata:
      labels:
        app.kubernetes.io/name: permit-service
        app.kubernetes.io/instance: release-name
    spec:
      containers:
        - name: permit-service
          image: "drivehub.azurecr.io/meateam/permit-service:v2.0.0"
          envFrom:
          - configMapRef:
              name: kd.config
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 256Mi
            