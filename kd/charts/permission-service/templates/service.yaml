---
# Source: kd/charts/permission-service/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: permission-service
  labels:
    app.kubernetes.io/name: permission-service
    helm.sh/chart: permission-service-0.1.0
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/managed-by: Tiller
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: permission-service
    app.kubernetes.io/instance: release-name